# @vipos/shared

Shared TypeScript types + Zod schemas antara `apps/web` dan `apps/backend`.

Akan diisi di task **P0-04: Type-safe API contract (OpenAPI + Zod)**
(lihat `docs/v3/workflow/phase_0_foundation.md`).

Untuk sekarang package ini placeholder — tidak ada source code.
