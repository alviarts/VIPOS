{
  "name": "@vipos/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types + Zod schemas antara apps/web dan apps/backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist",
    "test": "echo \"@vipos/shared: no tests yet\""
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^7.3.4",
    "zod": "^3.25.76"
  }
}
